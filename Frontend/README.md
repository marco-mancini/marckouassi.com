# Interface du site

La page du portfolio se trouve dans `index.html`. Les styles sont organisés dans `Design_System/styles/`, et `Index.css` importe les feuilles de style ainsi que les jetons partagés définis dans `Tokens.css`. Les interactions légères (apparitions et année courante) se trouvent dans `script.js`.
