# Frontend

La page du portfolio est dans `index.html`. Les styles sont dans `styles.css` et importent les tokens partagés depuis `Design_System/tokens.css`. Les interactions légères (apparitions et année courante) sont dans `script.js`.
