# Composants

Les composants React partagés sont définis avec un fichier `.tsx` et une feuille `.css` par composant. `Button.tsx` / `Button.css` donnent le modèle de départ.

Le site public actuel est une page HTML statique et ne compile pas ces composants. Ils sont prêts à être repris quand une interface React sera introduite.
