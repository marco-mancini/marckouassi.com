# Déploiement

Le workflow GitHub Actions reste dans `.github/workflows/pages.yml`, emplacement requis par GitHub. Il copie la page et ses interactions depuis `Frontend/`, ainsi que tout le Design System et les visuels de `Public/`, dans `_site` avant publication sur GitHub Pages.
